/** TODO(auth-ui): OAuth sign-in buttons (Google + Apple). */

export function OAuthButtons() {
  return (
    <p className="text-sm text-muted-foreground">
      OAuth buttons — auth-ui agent owns this component.
    </p>
  );
}
