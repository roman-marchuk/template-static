/** TODO(calculator): Calculator UI shell — keypad, display, history panel. */

export function CalculatorApp() {
  return (
    <p className="text-sm text-muted-foreground">
      Calculator UI — calculator agent owns this component.
    </p>
  );
}
